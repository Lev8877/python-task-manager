# Python Task Manager

Учебный проект для изучения Python Backend.

22.06.2026 День первый. Я закончил 3 упражнения и одно решение для свое проекта. Повторил самый базовый синтаксис Python, вспомнил конструкцию как округлять значия типа данных float -> :.2f и вспомнил что input() возвращает именно пустую строку даже если в консоле туда ничего не вписывать. 

22.06.2026 День второй. Я закончил 4 упражнения и решение одного проектного задания. Я повторил условный синтаксис. Нужно не эксперементировать с отсутсвием скобок когда используешь сразу два логических оператора or и and. Лучше просто всегда поставить скобки и не париться. Также не стоит забывать что когда применятеся скидка ее нужно брать от начальной цены, а не применять все скидки на уже измененную цену. Также стоит чаще использовать if / elif / else чем просто накидывать if'ы. 

23.06.2026 День третий. Я закончил 9 упражлений на * списки; * кортежи; * множества; * словари; * индексы и срезы; * основные методы коллекций. Также сделал решение для моего проекта где добавил возможность добавления нескольких задач сразу и работу через словари. Мне нужно помнить что в insert() в начале передается индекс первым аргументом, а вторым уже значение. pop() просто удаляет из массива жлемент по индексу переданный в аргумент. remove() тоже самое только в аргумент передается уже не индекс а элемент массива. index() вернет индекс элемента массива, в эту комманду передается элемент самого массива. Количество определенного значение это значит нужно определить сколько раз определенный элемент входит в определенную коллекцию. Кортеж можно распаковать таким удобным способом: x,y = one где one = (1,2) => x = 1, y = 2. Задание множества через фигурные скобки: sett = {1,2,3,4}. | логический оператор для объединения двух ммножеств в один. & логический оператор для нахождения пересечений элементов двух множеств. - логический оператор для отрицания одного множества из другого. ^ - логический оператор симметрической разницы - надет значения множеств в первом и втором но не их объединения. remove(a) удалит элемент a из множества но вызовет ошибку если такого элемента нет. discard(a) удалит элемент a из множества и не вызовет ошибку даже если такого элемента в коллекции нет. sort() - отсортировывает старый массив и ничего не возвращает, работает только с массивами. sorted() возвращает новый массив из отсортированной последовательности любого типа данных. user.get(a,n) - вернет значение словаря user по ключу a, если такого ключа нет ничего не вернет или вернет n если был передан второй аргемент что безопасней чем просто использовать квадратные скобики для получения значений из словаря. user.pop(a) - удалит ключ a из словаря user. user.keys(), user.values() - вернет массив из ключей/значений словаря user. user.items() - вернет словарь из пар кортежей (ключ,значение). user.copy - создаст копию словаря User, лучше передовать это в переменную. user.clear() полностью отчистит словарь и сделает его пустым. extend() в отличии от append() добавляет в массив не целый объект (например [2,3]) а каждый элемент отдельно из любой последовательности любого типа данных (то есть в начале 2, потом 3 из массива [2,3]).   

23.06.2026 День четвертый. Я повторил Темы: for; while; range; break; continue; перебор списков; перебор словарей; вложенные циклы. Сделал 9 задач и один достаточно нудное решение с бесконечным циклом в проекте. Мне нужно помнить о том что range() также можно распоковывать звездочкой *. Оказывается если после выполненного цикла for i in ... поставить else, то else выполнится в том случае если цикл выполнелся корректно. Если в цикле произошел break то else выполнятся не будет. Также нужно помнить что бесконечный циклы в консоле можно оборвать сочитанием клавиш ctrl + c прописав их в консоль. enumerate() работает также как и items(), то есть прописываем цикл for index, value in "массив", index примет индекс элементов массива а value соотвественно значения в списке соответственно индексам. 

24.06.2026 День пятый. Я повторил темы: объявление функций через def; параметры и аргументы; return; именованные аргументы;значения по умолчанию; локальная и глобальная область видимости. Я сделал 9 заданий на повторение и доработал свой проект, сделав его код намного более понятным и структурированным с помощь. функций. Оказывается у функций есть такое понятие как именованные аргументы. Это такие аргументы, которым можно задать значение по умолчанию при вызове функции через равно. Если этот аргумент не был вызван при вызове функции, то его значение по умолчанию внутри этой функции будет применено. При использовании f-строк лучше при обращению к ключу с помощью строки внутри это f-строки использовать одинарные кавычки. Двойные могут вызвать ошибку если кодить на более низких версиях питона чем сейчас. Есть такое понятие как глобальная переменная. Глобальную переменную можно объявить в функции через global и она перестанет быть локальной только для самой функции где она было объявлена. Такой способ можно применять но не желательно. Массивы и т.п. объявленные за пределами функциями уже являются глобальными и их можно изменять внутри функции.

24.06.2026 День шестой. Просто отработал 4 задачи на повторении всех мною изученных тем. Теперь могу переходить к более разнообразному Python. 

25.06.2026 День седьмой (Вторая неделя первый день по моему roadmap). Я вспомнил что такое модули в Python, как их импортировать в другие файлы, виды импорта, какое значение приобретает __name__ при запуске кода напрямую одновременно с импортированным модулем. Что происходит при импорте. Познакомился с структурой if __name__ == "__main__": и понял зачем она нужна. Осознал что лучше в main фалйе всегда писать код в функции main, и через конструцию if __name__ == "__main__": main() вызывать эту функцию. Это очень хорошее решение для проектов. Также вспомнил определение стандартных библиотек и библиотеки которые чаще всего испольщуются при написании кода. По итогу я решил 3 задачи для закрепления и доработал свой проект. Я создал два отдельных файла tasks.py и utils.py. В utils.py засунул вспомагательные функции для упрощения кода, а в tasks.py основные функции для работы код. Смысл main остался лишь в выводе меню и взаимодействиями с функциями в tasks.py что значительно улучшило читабельность кода. 

26.06.2026 День восьмой (Вторая неделя второй день по моему roadmap). Я изучил формат данных JSON. Понял что он из себя бредставляет формат записи данных, который можно преобразовать в любой язык программирования, и не обязательно словари а вообще любой тип данных. Изучил json.dump(), json.load(). Изучил конструкцию with open(...), понял как читать файлы построчно или записать все строки в массив. Также понял смысл кодировок и зачем они нужны. Сделал 2 задания на изучение JSON и доработал свой проект. Теперь он не зависит от единиченого включения программы, теперь им можно пользоваться всегда так как все записанные задачи сохраняются в отдельный файл tasks.json. 

27.06.2026 (ночь) День девятый (Вторая неделя третий день по моему roadmap). Я изучил что такое исключения в python. Изучил что такое try, except, else, finally, raise и их смысл. Также выучил названия некоторых типичных системных ошибок по типу ValueError. Я выполнил 4 заданий на эти конструкции и сделал защиту с помощью них для своего проекта. Теперь моя программа не падает от неправильно введенных символов (буквы заместо чисел) и продолжает работу постоянно. Также защита если не найдется путь до файла JSON или сам файл будет поврежден или пуст. 

27.06.2026 День десятый (Вторая неделя четвертый день по моему roadmap). Я изучил что такое вирутальное окружение и зачем оно нужно. Также изучил переменные окружения, консольные команды к ним. Понял что такое .venv и зачем нужен requirements.txt. Выполнил одно задание на проверку переменной окружения, действительно переменная была только в одной сессии терминала. По поводу проекта добавил папку .venv для виртуального оркужения и текстовый файл requirements.txt. Там пока что только одна библиотека просто для теста системы. А и добавил .gitignore так чтобы вы не сможете увидеть эту папку, как и другие которые вам видеть не нужно. 

27.06.2026 День одинадцатый (Вторая неделя пятый день по моему roadmap). Я изучил comprehension (генераторы) на все изменяемые типы данных, вместе с фильтрацией (if/else в генераторах). Также вспомнил распаковку последовательностей с помощью звездочки, причем не только при присваивании переменных, но это оказывается можно делать и в функции, и даже две звездочки можно поставить перед переменной чтобы распоковать словарь. Прошел *args и **kwargs, также оказывается можно сразу в функции использовать и обычный аргумент, и *args, и **kwargs. Выучил определения изменяемых/неизменяемых объектов и какие типы данных входят и в те и в те. Также разобрался с проблемой при ссылках на один список, ее смысл и как оно решается. Выучил зачем нужен copy() и copy.deepcopy() чтобы разобраться с проблемой ссылок на внутренние объекты. Сделал 4 задания на отработку этих тем и дополнил свой проект функцией, позволяющей вывести все срочные задачи. Это сильно на функционал не влияет просто нужно было отработать генераторы в проекте. 

29.06.2026 (Ночь) День двенадцатый (Третья неделя первый день по моему roadmap). Знакомый позвал меня ему помочь в организации предложения его супреги прямо в аэропорту, я не успел сегодня по плану сделать еще один блок изучения проги :sob:. Но за часть этой ночи я изучил основу ООП. Что такое классы и зачем они нужны, объекты, атрибуты, методы, как их задавать и манипулировать с ними. Что такое __init__ и когда он выполняется. Что такое self. Естественно на этом изучение ООП не заканчивается, а только начинается. Я отработал 3 задания на создания классов, изменение атрибутов и манипуляции с объектами. Также в своем проекте создал файл task_model.py и туда добавил класс Task с методами которыми можно потом заменить код в tasks. Пока на этом все, завтра родители уедут на работу, никто меня не позовет организовывать предложения, я сделаю все как положено. Так же мне стоит помнить что в методах класса нужно не забывать писать первым аргументом self. 

30.06.2026 День тренадцатый (третья неделя день второй по моему roadmap). Я изучил что такое __str__, __repr__, dataclass, композиция, наследование, super(), переопределение метода, значения по умолчанию. Уже начинаются темы которые не так просто запоминаются и сегодня на anki ушло порядка полтора часа, но в след раз должно уже быть намного меньше карточек. Я сделал 4 задания на отаботку это тем и переделал класс в task_model с dataclass. В след блоке изучения ООП возможно уже переделаю свой менеджер задач под созданный мною класс. 

01.07.2026 День четырнадцатый по моему roadmap. Это был очень объемный для меня блок. Я изучил type hints, типизацию, итераторы, генераторы, декораторы, объекты в коллекции, конвертация объектов в словари и обратно. Выполнил 21 задание на генераторы, итераторы, декораторы и переделал свой проект. Теперь там не словари а объекты класса Task. Все в новинку так что предстоит еще много чего отработать.